import './Navbar.css';

const Navbar = props => {
    return (
        <div>
            <nav>
                <a href="#">Transit Hub</a>
                <button>Login</button>
            </nav>
        </div>
    )
}

export default Navbar;